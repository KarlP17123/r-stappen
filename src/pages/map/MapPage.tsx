import { useState } from 'react'
import { PageHeader } from '../../components/common/PageHeader'
import { AppLayout } from '../../layouts/AppLayout'

export function MapPage() { const [active, setActive] = useState('Global'); return <AppLayout><PageHeader title="World Opinion Map" subtitle="See how people across the world are voting in real time." /><div className="map-controls">{['Global', 'Technology', 'Politics', 'Sports'].map(item => <button className={active === item ? 'selected' : ''} onClick={() => setActive(item)} key={item}>{item}</button>)}</div><section className="world-map"><div className="map-art">{[['72%', 'North America'], ['58%', 'Europe'], ['81%', 'Asia'], ['64%', 'South America'], ['47%', 'Oceania']].map(([value, country], index) => <button className={`country-dot d${index}`} onClick={() => setActive(country)} key={country}><b>{value}</b><small>{country}</small></button>)}</div><aside className="map-panel"><span className="category-badge">{active}</span><h2>{active === 'Global' ? 'Is AI good for society?' : `${active} opinions`}</h2><div className="big-stat">72% <small>agree</small></div><div className="progress"><i style={{ width: '72%' }} /></div><p>125,430 votes from 195 countries</p></aside></section></AppLayout> }
